<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin | Users</title>
    <link rel="stylesheet" href="../styles/colors.css">
    <link rel="stylesheet" href="../styles/fonts.css">
    <link rel="stylesheet" href="../styles/transfer.css">
    <link rel="stylesheet" href="../styles/table.css">
    <script src="../scripts/admin_updateuser.js" defer></script>
</head>
<body>
    <div class="container">
        <button onclick="window.location.href='/acctadmin/dashboard'" class="back-btn">Back to Dashboard</button>

        <table>
            <thead>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Status</th>
                    <th>Transaction ID</th>
                    <th>Transaction Type</th>
                    <th>Flagged</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <form class="update-form" method="post" action="">
                <% users.forEach(user => { %>
                    <tr>
                        <td>
                            <span id="first_name_<%= user.id %>"><%= user.first_name %></span>
                        </td>
                        <td>
                            <span id="last_name_<%= user.id %>"><%= user.last_name %></span>
                        </td>
                        <td>
                            <select name="status" id="status_<%= user.id %>">
                                <option value="active" <%= user.is_active ? 'selected' : '' %>>Active</option>
                                <option value="inactive" <%= !user.is_active ? 'selected' : '' %>>Inactive</option>
                            </select>
                        </td>
                        <td>
                            <span><%= user.transactionid %></span>
                        </td>
                        <td>
                            <span><%= user.type %></span>
                        </td>
                        <td>
                            <span><%= user.flagged ? 'Yes' : 'No' %></span>
                        </td>
                       
                        <td>
                            <button type="button" class="submit-btn" data-user-id="<%= user.id %>">Update</button>
                        </td>
                    </tr>
                <% }) %>
                </form>
            </tbody>
        </table>
    </div>
</body>
</html>
